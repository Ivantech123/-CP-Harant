node_modules/
dist/
*.log
.env
.env.local
.env*.local

# .next/ intentionally NOT ignored — built locally and committed for Vercel
# but exclude the webpack cache (large binary files, not needed by Vercel)
.next/cache/
